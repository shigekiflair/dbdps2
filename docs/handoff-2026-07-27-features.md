# 実装メモ（2026-07-27）：新機能4点

このチャットで実装した4つの機能の引き継ぎメモです。マージ前に必ずセットアップ手順を確認してください。

---

## 📌 運用ルール（毎回必ず守ること）

**コードに変更があり、コミット・プッシュする時は、必ず以下の4つをこの順番でまとめて案内する。**

```bash
npm run test
npm run smoke -- https://dbdps2.vercel.app
git add .
git commit -m "（変更内容）"
git push
```

- `npm run test`と`npm run smoke`は、`git add`より前に実行する（コミット前に壊れていないか確認するため）
- 手動で忘れやすい作業なので、コミット・プッシュのコマンドを案内するときは必ずこの2つも一緒に出力し、実行してもらう
- この2つは新しいチャットセッションでも省略しない（このドキュメントを確認すれば分かるようにするため、ここに明記している）

---

## 実装した機能

1. **お気に入り・マイページ**（`/mypage`）
2. **停電イースターエッグの予告演出＋演出簡略化/ミュートトグル**（`random-select-tool.tsx`）
3. **イベントトリガー型（`trigger_internal`）の専用UI**（セリフガチャ・お告げガチャ等の1ボタン開示型）
4. **予想・ベッティング型（`betting`）**（裁判ガチャ／視聴者オッズ予想戦。配信者がお題を出し、視聴者が投票、配信者が正解を確定するとランキングが更新される）

## セットアップ手順（必須）

### 1. DBマイグレーション

`db/schema.ts`に以下を追加しました。ローカルで生成・適用してください。

```bash
npm run db:generate
npm run db:migrate
```

追加したテーブル/カラム：
- `plan_favorites`（お気に入り）
- `betting_rounds` / `betting_votes`（予想・ベッティング型）
- `users.isAdmin`（配信者本人かどうかのフラグ。下記2で使う）

### 2. 配信者アカウントに `isAdmin` を立てる（予想・ベッティング型に必須）

予想・ベッティング型の「お題を作る／締切る／正解を確定する」操作は、`session.user.isAdmin === true` のユーザーだけが行えます。誰でもログインできてしまうと荒らされる可能性があるための制限です。

マイグレーション後、一度Twitch/Googでログインしてアカウントを作成 → Neonのコンソールか `psql` で以下を実行してご自身のアカウントを管理者にしてください。

```sql
update "user" set "isAdmin" = true where email = 'あなたのメールアドレス';
```

これを忘れると、予想・ベッティング型のページで「お題を作る」フォームが表示されません（＝ホストとして認識されていない状態）。

## 実装メモ・既知の制約

- **予想・ベッティング型はPusher等のリアルタイム基盤を使わず、6秒間隔のポーリングで実装しています。** 視聴者の投票・配信者の締切/結果発表は最大6秒遅れで反映されます。体感速度が問題になるようなら、次のステップとしてPusher導入をご提案します（`package.json`に依存追加が必要）。
- 投票の得票数・割合は、ラウンドが「締切」または「結果発表済み」になるまで視聴者には見えません（先に投票した人の意見に引っ張られないようにするため）。配信者だけは締切後に見えます。
- ランキングは企画(plan)ごとの累積正解数です。ログインしていない視聴者は名前が「匿名視聴者」表示になります（Cookieでは名前を判別できないため）。ログインすれば名前が出ます。
- イベントトリガー型（`trigger_internal`）は、`customPool`の配列を「山札」として扱い、出し切るまで同じ項目が重複しない設計です。OBS常時表示リンクも発行できます（`target_pick`と同じ`/overlay/[code]`の仕組みを流用）。
- 予告演出（チラ見せ）は、停電になる時は必ず、それ以外の時も約8%の確率で「ガセ」として発生します（本物かどうかは演出が終わるまで分からない、という設計）。この確率は`random-select-tool.tsx`の`maybeTease()`内の`0.08`で調整できます。
- 効果音は音声ファイルを追加せず、Web Audio APIでその場合成しています（`playSound()`）。素材が必要な場合は差し替えも可能です。

## 未実装（次の候補）

- 予想・ベッティング型の視聴者参加タブへの表示（`plan-filter-bar.tsx`のフィルタタブに「視聴者参加」はあるが`betting`タイプでの絞り込みは動作確認未）
- Pusherによる完全リアルタイム化
- お気に入りの並び替え（現状は追加順のみ）

---

# 追記（同日）：企画作成機能 Phase1（配信者自身が企画を作れる機能）

## 実装した内容

1. **文字列リスト型企画の作成フォーム**（`/plans/new` → `/plans/new/list?type=...`）
   トリガー・チェイン・ロールプレイ・エスカレーション・データ蓄積・ベッティングの6タイプに対応。ログイン必須。
2. **ティア表**（新しい企画タイプ`tier_list`）
   `/plans/new/tier-list` でキラーをS/A/B/C/D（自由に追加・削除・改名・色変更可）にドラッグ&ドロップで振り分けて作成。ネイティブHTML5 Drag & Drop APIで実装しており、追加ライブラリは使っていません（タップで割り当てるフォールバックUIも用意し、スマホでも操作可能）。作成後は`/plans/[slug]/edit`で編集できます（本人のみ）。
3. **公開範囲**：`private`（自分だけ）/ `unlisted`（リンクを知っている人だけ）の2択。`public`（「みんなの企画」一覧への掲載）はPhase2で解禁予定なので、今回作成フォームには選択肢を出していません。
4. **マイページ**に「自分の企画」セクションを追加（一覧・見る・編集・削除）

## DBスキーマ変更点（マイグレーション必須）

- `plans.type` に新しい値 `tier_list` を追加
- 新しいenum `plan_visibility`（`private`/`unlisted`/`public`）を追加
- `plans`テーブルに`created_by`（users.idへの外部キー、null許容）と`visibility`（デフォルト`public`）カラムを追加
  - **重要**：既存の運営キュレーション企画（96件超）は`created_by`がnullのままなので、今回の変更で表示や挙動が変わることはありません。`getPublishedPlans()`は`created_by IS NULL`のものだけを対象にするよう変更済みです
- `users`テーブルに`isAdmin`カラム（前回追加済み。今回の機能とは無関係）

いつも通り

```bash
npm run db:generate
npm run db:migrate
```

を実行してください。

## 注意点・既知の制約

- 企画作成はログイン必須です（Twitch/Googleアカウントがあれば誰でも作成可）。`isAdmin`は不要です（ベッティング型のホスト操作だけがisAdmin必須）
- ティア表の編集フォームのみ実装しています。文字列リスト型（トリガー・チェイン等）はまだ編集機能がなく、作り直す場合はマイページから削除→再作成する運用になります
- スラッグ（URL）はタイトルから自動生成しています。日本語タイトルの場合、英数字が拾えないと`plan`のような汎用スラッグになります（`lib/slug.ts`）
- 「みんなの企画」タブ（public企画の一覧・検索）はまだ実装していません。Phase2の想定通りです

---

# 追記（同日）：予想・ベッティング型を競馬風（単勝/2連単/3連単）に拡張、ポイントランキング新設

## 実装した内容

1. **ベッティング型に「モード」を追加**：単勝（1つ的中/10pt）・2連単（1位2位を順番通り/30pt）・3連単（1位2位3位を順番通り/50pt）。実際の競馬と同じく**完全一致のみ的中**扱いで、部分点はありません
2. **候補（馬にあたるもの）は自由入力とDBDキャラクター（キラー/サバイバー）の両方に対応**。お題作成フォームでキャラクターをチェックして候補に追加できます
3. **ポイントランキングを新設**：的中すると`point_transactions`という履歴テーブルに加点が記録されます。`/ranking`でサイト全体の累積ランキングが見られます（企画ごとのランキングは各ベッティング企画のページ内にも引き続き表示）

## DBスキーマ変更点（破壊的変更あり・マイグレーション必須）

- `betting_rounds`：`mode`カラム追加、`correct_option_id`(text)を廃止し`correct_picks`(jsonb配列)に置き換え
- `betting_votes`：`option_id`(text)を廃止し`picks`(jsonb配列、順序付き)に置き換え、`points_awarded`カラム追加
- 新テーブル`point_transactions`（サイト全体のポイント履歴。今後お気に入り数連動などの別ランキングにも使い回せる汎用設計）

**注意**：既存のベッティング企画で投票済みのデータがあった場合、`option_id`の値は引き継がれません（新しい`picks`配列形式に変換されないため）。まだ本格的な運用前とのことなので影響は無いと思いますが、念のためお伝えします。

```bash
npm run db:generate
npm run db:migrate
```

## 未実装・今後の候補

- ティア表のキャラクター画像表示（運営元の許諾待ち。画像ファイルが用意でき次第対応します）
- ポイントの使い道（バッジ・称号・特典との連動など）
- お気に入り数と連動した「人気企画ランキング」（ポイントランキングと同じ`point_transactions`の設計を応用できます）

---

# 追記（同日）：セルフレビューで挙げた優先度1・2・5・3に対応

## 1・2: 匿名Cookieによるデータ消失・不正投票への対応

- **ポイント付与はログイン済みユーザーのみ**に変更しました（`lib/betting.ts`の`resolveRound`）。匿名投票自体は引き続き可能ですが、的中してもポイントは付与されません。Cookieを消して何度も投票し直す「うまみ」を無くす狙いです
- `/mypage`・`/ranking`・投票画面に、未ログイン時の注意書きとログイン導線を追加しました

## 5: ベッティングの過去質問履歴

- `betting_rounds`から過去の質問（最新以外）を取得する`getRoundHistory`を追加し、企画ページ内に「過去の質問（n件）」として表示するようにしました

## 3: 最低限のモデレーション機能

- 新テーブル`plan_reports`を追加
- ユーザー作成企画のページに「この企画を通報する」ボタンを追加（作成者本人には表示されません）
- 管理者専用ページ`/admin/reports`を追加。未対応の通報一覧を確認し、「問題なし（却下）」または「企画を削除する」を選べます
- 企画一覧ページのヘッダーに、管理者にだけ「通報（件数）」リンクを表示するようにしました

## マイグレーション必須

`plan_reports`テーブルを追加したので、いつも通り

```bash
npm run db:generate
npm run db:migrate
```

が必要です。

---

# 追記（同日）：管理者削除をソフトデリート化（優先度1対応）

## 実装内容

- `plans`テーブルに`deleted_at`カラムを追加。削除は物理削除ではなく、この日時を立てるだけに変更
- `getPublishedPlans`・`getPlanBySlug`・`getUserPlans`は自動的に削除済みを除外
- `/admin/trash`を新設：削除済み企画の一覧・復元・完全削除（こちらは取り消し不可）
- `/admin/reports`から「ゴミ箱を見る」に遷移可能

マイページからユーザー自身が削除する場合も同じ仕組み（`deleteUserPlan`）を使っているので、誤って自分の企画を消してしまった場合も管理者が`/admin/trash`から復元できます。

## マイグレーション必須

```bash
npm run db:generate
npm run db:migrate
```

---

# 追記（同日）：通報の連投制限（優先度3対応）

DBスキーマの変更なし（既存の`plan_reports`テーブルのタイムスタンプを利用）。

- 同じ人が同じ企画を重複通報できないようにブロック（未対応の通報が残っている間は再通報不可）
- 1時間あたり合計5件までの通報に制限（複数企画にまたがる連投荒らし対策）
- 制限に引っかかった場合、通報フォームにその理由をそのまま表示するようにした

---

# 追記（同日）：軽量テストの導入（優先度4対応）

E2Eではなく、この規模に見合った2種類の軽量テストを導入しました。DBスキーマ変更はありません。

## 1. ユニットテスト（ポイント計算・権限判定）

- `lib/betting-rules.ts`：的中判定(`isExactMatch`)・ポイント計算をDB非依存の純粋関数として切り出し
- `lib/permissions.ts`：ホスト権限判定(`canHostPlan`)・閲覧権限判定(`canViewPlan`)を同様に切り出し
- `lib/betting.ts`・`app/betting/actions.ts`・`lib/plans.ts`は、これらの純粋関数に処理を委譲する形にリファクタリング（挙動は変えていません）
- `tests/betting-rules.test.ts`・`tests/permissions.test.ts`：Node.js標準の`node:test`を使用（Jest/Vitest等の新規依存追加なし）

実行方法：

```bash
npm run test
```

**注意**：PowerShellはワイルドカード（`*.test.ts`）を展開してくれないため、`package.json`の`test`スクリプトはテストファイルを1つずつ明示的に列挙する形にしています。今後新しいテストファイルを追加したら、`package.json`の`test`スクリプトにもファイル名を追記してください。

## 2. デプロイ後スモークテスト

`scripts/smoke-test.ts`：`/plans`・`/mypage`・`/ranking`・`/api/auth/providers`・`/api/auth/session`にアクセスして200が返るか確認するだけの軽量スクリプト。今回のAUTH_SECRET未設定事故のような「デプロイもマイグレーションも成功しているのに認証だけ全滅」というパターンを検知できます。

手動実行：

```bash
npm run smoke -- https://dbdps2.vercel.app
```

## 3. 自動実行について（GitHub Actionsは使わない方針に変更）

当初はGitHub Actionsで自動化する想定でしたが、リポジトリがPrivateでActions無料枠を使い切っていたため、**`.github/workflows/`は削除し、手動実行に切り替えました**。

**デプロイのたびに、以下の2つを手元のPowerShellで実行してください。**

```bash
npm run test    # ユニットテスト(数秒で終わります)
npm run smoke -- https://dbdps2.vercel.app   # 本番URLへの疎通確認
```

手間は増えますが、費用も公開リスクもゼロです。もし後日Actionsを使いたくなった場合（無料枠がリセットされた後、リポジトリをPublicにした後、従量課金を有効にした後など）は、上記の`npm run test`・`npm run smoke`コマンドをそのままGitHub Actionsのワークフローに組み込むだけで自動化できます。

---

# 追記（同日）：isAdmin付与のUI化（優先度5対応）

DBスキーマ変更なし。マイグレーション不要です。

- `/admin/users`を新設：ログインしたことのあるユーザー一覧を表示し、管理者への昇格・降格をワンクリックで行える
- 自分自身の管理者権限は、このUIからは外せない安全装置つき（誤操作でロックアウトするのを防ぐため。本当に外したい場合はDBを直接操作する運用のまま）
- `/admin/reports`から「管理者の管理」「ゴミ箱」に相互リンク

これで最初の1人（今のあなたのアカウント）だけはSQLで手動設定が必要ですが、それ以降の管理者追加はUIから行えます。

---

# 追記（同日）：ティア表の画像対応（仮アイコン付き）

DBスキーマ変更なし（`characters.icon_url`は元々存在していたカラムをそのまま使用）。マイグレーション不要です。

## 実装内容

- `components/character-avatar.tsx`を新規作成：`icon_url`が設定されていれば画像を表示、**未設定の場合はキャラ名から自動生成した色付き頭文字バッジを表示**（画像ファイル一切不要）。色はキャラ名から決定的に計算しているので、同じキャラは常に同じ色になります
- ティア表の閲覧画面（`tier-list-tool.tsx`）・作成/編集画面（`tier-list-editor.tsx`）の両方に組み込み済み

## 今後、本物の画像を反映する方法

運営元の許諾が下りて画像ファイルが用意できたら、`characters`テーブルの該当キラーの`icon_url`列に画像のURL（`public/characters/xxx.png`のような相対パス、または外部ホスティングのURL）を入れるだけで、自動的に仮アイコンから本物の画像表示に切り替わります。コード側の変更は不要です。

---

# 追記（同日）：サーバーアクションのエラーメッセージが本番で消えるバグを修正（重要）

## 何が起きていたか

Next.jsは本番ビルドで、Server Action内で`throw new Error("メッセージ")`した内容を**セキュリティ上の理由で自動的に握りつぶし**、以下のような汎用メッセージに差し替えます。

```
An error occurred in the Server Components render. The specific message is omitted in production builds...
```

これは開発環境（`npm run dev`）では発生せず、**本番(Vercel)でのみ再現する**ため、動作確認だけでは気づけませんでした。今回`/plans/new/tier-list`でタイトル未入力のまま送信した際にこの汎用エラーが表示され、発覚しました。

影響範囲は「このチャットで実装した、バリデーション/権限エラーをthrowしていたServer Action全部」でした。つまり企画作成・ベッティングの投票や締切・通報・削除・管理者操作など、これまで作ってきた「〇〇してください」という赤枠メッセージの多くが、実際には本番でこの汎用メッセージに化けていた可能性があります。

## 直した内容

対象となる全てのServer Actionを、**`throw`ではなく戻り値`{ error?: string }`でエラーを返す**方式に統一しました。呼び出し側のクライアントコンポーネントも、`try/catch`ではなく`if (result.error)`でチェックする形に合わせて修正しています。

修正したファイル：
- `app/plans/new/actions.ts` / `components/plan-builder/string-list-form.tsx` / `tier-list-form.tsx`
- `app/betting/actions.ts` / `components/tools/betting-tool.tsx`
- `app/plans/[slug]/report-actions.ts` / `components/report-plan-button.tsx`
- `app/mypage/actions.ts` / `components/my-plan-card.tsx`
- `app/admin/reports/actions.ts` / `app/admin/reports/report-row.tsx`
- `app/admin/trash/actions.ts` / `app/admin/trash/trash-row.tsx`
- `app/admin/users/actions.ts` / `app/admin/users/user-row.tsx`

`app/plans/actions.ts`（ランダムセレクト等の既存機能）や`app/favorites/actions.ts`は、投げているエラーが「本来起こらないはずの異常系(plan not found等)」で、かつクライアント側でもメッセージを画面表示していないため、今回は対象外としています。

## 今後の注意点（新しいServer Actionを作る時のルール）

**Server Actionの中でバリデーションエラーをユーザーに見せたい場合は、`throw`ではなく`return { error: "メッセージ" }`の形で返すこと。** `throw`していいのは「本当に起きてはいけない異常系」で、かつユーザーに詳細を見せる必要が無い場合だけ。

マイグレーションは不要です（ロジックの修正のみ）。

---

# 追記（同日）：ゲームデータ管理機能（コラボレーター権限）

## 実装内容

DBDは約3ヶ月ごとにキャラクター・パーク・アドオン・マップが更新される（コラボ終了時に固有パークが共通パークに変わることもある）ため、管理者だけでなく信頼できる協力者にもデータ更新を任せられるようにしました。

- `users`テーブルに`isCollaborator`カラムを追加（`isAdmin`とは独立）
- `/admin/users`から、管理者権限とは別に「コラボレーター」権限を付与できるように拡張（自分自身への付与/剥奪も可能、管理者権限より慎重さは不要なため）
- `/admin/game-data`を新設。以下4種類のデータを追加・編集できます
  - **キラー / サバイバー**：新キャラの追加、名前・チャプター等の編集
  - **パーク**：新パークの追加、**「固有キャラ」を未選択にすると共通パークに変更**（コラボ終了時の切り替えはこれで完結します）
  - **アドオン**：新アドオンの追加・編集（全890件程度あるため、一覧表示はせず検索でヒットしたものだけ表示する設計にしています）
  - **マップ**：新マップの追加・編集
- ヘッダーナビゲーションに、管理者/コラボレーターにだけ「ゲームデータ管理」リンクを表示

## あえて今回作らなかったもの

- **一般ユーザーによる投稿・提案機能**：以前のアドオンデータ収集で誤情報が大量に混入した経緯があるため、今回は見送りました。荒らし・誤情報対策が別途必要になるため、必要になったら別途相談してください
- **削除機能**：キャラ・パーク・アドオン・マップは他のデータから参照されている（外部キー）ため、誤って消すとデータ破損につながります。追加・編集のみとし、削除は意図的に実装していません。本当に削除したいデータがあれば、都度DBを直接操作してください
- **アイテム・オファリングの管理画面**：ほとんど変化しないデータ（アイテムは5種で固定など）のため今回は見送りました。必要になれば同じパターンで追加できます

## マイグレーション必須

```bash
npm run db:generate
npm run db:migrate
```

---

# 追記（同日）：管理者/コラボレーター用のまとめメニューを追加

DBスキーマ変更なし。マイグレーション不要です。

- `components/admin-menu.tsx`を新規作成。`/plans`・`/mypage`・`/ranking`のヘッダーに「管理メニュー」ドロップダウンを追加しました
- 中身は権限に応じて自動的に出し分けられます：ゲームデータ管理（管理者・コラボレーター）／通報の確認・ゴミ箱・管理者の管理（管理者のみ）
- **セキュリティ上の注意点**：このメニューはあくまで見た目上の利便性であり、実際のアクセス制御は各`/admin/*`ページ側のサーバーサイドチェック（`session.user.isAdmin`等）で行っています。メニューを出す/出さないを変えても、ページ自体の保護レベルは変わりません

---

# 追記（同日）：ゲームデータ管理フォームにラベルを追加

DBスキーマ変更なし。マイグレーション不要です。

プレースホルダーだけに頼っていたため、既存データを編集する際（値が既に入っている状態）に何の項目か分からなくなっていた不具合を修正しました。`components/game-data/field.tsx`という共通ラベルコンポーネントを作り、キラー/サバイバー・パーク・アドオン・マップの4つのフォーム全てに適用しています。

---

# 追記（同日）：UI/UXレビュー Phase 1（非技術者向け改善: 確認ダイアログ・用語の平易化）

DBスキーマ変更なし。マイグレーション不要です。

## やったこと

1. **`window.confirm`を全廃し、サイトデザインに合わせた独自の確認ダイアログに統一**
   - `components/confirm-dialog.tsx`（`ConfirmProvider`/`useConfirm`）を新規作成し、`app/layout.tsx`でアプリ全体に適用
   - 対象：ランダムセレクトの使用履歴リセット、進捗トラッキングのリセット、エスカレーション型のリセット、ターゲット指定型のやり直し、予想投票の正解確定、管理者/コラボレーター付与、通報企画の削除、ゴミ箱からの完全削除
   - ブラウザ標準ポップアップは詐欺サイトのように見えて信頼感を損ねるため、というのが変更理由です

2. **「slug」等の専門用語を平易な言葉に置き換え**
   - ゲームデータ管理フォームの「slug」→「管理用の名前（半角の英字・数字・ハイフンだけを使った、他と被らない名前）」
   - 権限エラーメッセージも「〇〇でのみ行えます」のような硬い言い回しから、「〇〇できるのは本人だけです」のような分かりやすい言い回しに変更

## 次のPhase

- Phase 2: サイト説明・使い方の導入セクション、ナビゲーション整理
- Phase 3: 投票直後の即時フィードバック、ティア表操作の視覚的な説明強化
- Phase 4: モバイル実機での検証

---

# 追記（同日）：UI/UXレビュー Phase 2（サイト説明・ナビゲーション整理）

DBスキーマ変更なし。マイグレーション不要です。

## やったこと

1. **`/plans`にサイト説明を追加**：ロゴ下にタグライン（「Dead by Daylight 配信企画ポータル」）、その下に「何のサイトか・ログイン無しでも遊べる・ログインすると何が変わるか」を2〜3文で説明するカードを追加しました
2. **死んだリンクを削除**：`/plans`と`/mypage`のヘッダーにあった「ツール」というリンクは、実装されていない`/tools`ページへのリンクで、クリックすると404になる状態でした。これは元々あった未実装のリンクだったので削除しました

## 次のPhase

- Phase 3: 投票直後の即時フィードバック、ティア表操作の視覚的な説明強化
- Phase 4: モバイル実機での検証

---

# 追記（同日）：UI/UXレビュー Phase 3（操作の即時フィードバック・視覚的な説明強化）

DBスキーマ変更なし。マイグレーション不要です。

## やったこと

1. **投票直後の即時フィードバック**：予想投票で投票ボタンを押すと、ポーリング（最大6秒）を待たずに「✓ 投票しました」がその場ですぐ表示されるようにしました。選択肢を変更すると表示が消え、再投票を促します
2. **ティア表の操作説明を視覚化**：「ドラッグ&ドロップしてください」という1行のテキストだけだった説明を、PC操作・スマホ操作(①タップ→②タップ)を分けて番号付きで見せるカードに変更しました

## 次のPhase

- Phase 4: モバイル実機での検証（最後のPhase）

---

# 追記（同日）：ティア表、タッチ端末でのスクロール競合を修正

DBスキーマ変更なし。マイグレーション不要です。

キラーの要素にPC向けの「ドラッグ可能」設定を常時つけていたため、タッチ操作時に画面スクロールと競合し、タップ操作が正しく機能しない不具合がありました。`window.matchMedia("(hover: none), (pointer: coarse)")`でタッチ端末を検出し、タッチ端末では`draggable`属性自体を外してタップ操作に一本化しました。操作説明も、検出結果に応じてPC向け/タッチ向けのどちらか一方だけを表示するようにしています。

---

# 追記（同日）：キャラアイコンの相対パス404を修正

DBスキーマ変更なし。マイグレーション不要です。

DBに保存されていた画像パス（例: `killers/the-wraith.png`）の先頭に`/`が無く、今開いているページ基準で相対解決されて`/plans/new/killers/the-wraith.png`のような誤ったURLになり、常に404になっていました。表示上は仮アイコンにフォールバックしていたため壊れて見えてはいませんでしたが、コンソールに404が出続けていました。

`components/character-avatar.tsx`に正規化処理を追加し、DBの値がどんな形式でも(先頭`/`無し、`http`から始まる等)サイトルート基準で正しく解決されるようにしました。DB側のデータはそのままで問題ありません。

---

# 追記（同日）：favicon追加

DBスキーマ変更なし。マイグレーション不要です。

`app/icon.svg`を追加しました（既存のブランドロゴマークを流用）。Next.jsのApp Routerはこのファイルを自動検出してfaviconとして使うので、他に設定は不要です。これで`favicon.ico`の404が消えます。

---

# 追記（同日）：UI/UXレビュー Phase 4（モバイル実機で見つかった問題を修正）

DBスキーマ変更なし。マイグレーション不要です。

## 1. ヘッダーのレスポンシブ対応・共通化

`/plans`・`/mypage`・`/ranking`にそれぞれ別々に書かれていた（＝スマホ幅で潰れていた）ヘッダーを、`components/site-header.tsx`という共通コンポーネントに統一しました。

- 通常幅：今まで通り横並びナビ
- スマホ幅（`sm`未満）：「☰」ボタンでナビをまとめて開閉する形に変更（JSは使わず`<details>`要素で実装）
- 企画詳細ページ（`/plans/[slug]`）は「← 企画一覧へ」の戻りリンクしか無く、他のページへの移動手段が無かったため、同じヘッダーを追加しました

## 2. ランダムセレクトのキャラ/アイテムカードがスマホで縦長すぎる問題

キャラクター・アイテムを選ぶカードが、PC幅では固定幅（サイドバー的な配置）になる一方、スマホ幅ではその制限が外れて画面幅いっぱいの大きな正方形ブロックになっていました。スマホ幅では「アイコン＋名前」を横並びのコンパクトな行として表示するように変更し、縦方向の圧迫を減らしました（PC幅の見た目は変えていません）。

## Phase 4は以上で一区切りです

他にも実機で気になる箇所があれば、都度教えてください。

---

# 追記（同日）：文字列リスト型企画の編集機能を追加

DBスキーマ変更なし。マイグレーション不要です。

これまで「文字列リスト型（トリガー・チェイン・ロールプレイ・エスカレーション・データ蓄積・ベッティング）には編集機能がまだ無く、作り直すしかない」という制約がありましたが、これを解消しました。

- `app/plans/new/actions.ts`に`updateStringListPlan`を追加
- `StringListPlanForm`が作成/編集の両方に対応（`TierListPlanForm`と同じmode切り替えパターン）
- `/plans/[slug]/edit`が、企画タイプに応じて自動的にティア表用フォーム・文字列リスト用フォームのどちらかを出すように対応
- 企画ページ・マイページのカードに出る「編集する」リンクを、対応する全タイプで表示するように変更

これで、たとえば「性格ロールガチャ」に配信中に思いついたお題（英語禁止・上級者のフリをした配信、など）を、テキストエリアに1行追加するだけで足せるようになりました。

---

# 追記（同日）：管理者による運営キュレーション企画の編集を解禁

DBスキーマ変更なし。マイグレーション不要です。

これまで`updateUserPlan`/`deleteUserPlan`は「作成者本人(createdBy)と一致するかどうか」だけで権限判定していたため、`createdBy`が無い運営キュレーション企画（「ランダムセレクト」等）は、管理者であっても編集・削除できませんでした。

`lib/permissions.ts`の`canHostPlan`（管理者 or 作成者本人）を使うように変更し、管理者はどの企画に対しても編集・削除操作ができるようにしました。これにより、既に編集フォームがある7タイプ（ティア表・トリガー型・チェイン型・ロールプレイ型・エスカレーション型・データ蓄積型・ベッティング型）の運営企画は、`/plans/[slug]/edit`から管理者が直接編集できます。

---

# 追記（同日）：ターゲット指定型企画の編集機能を追加

DBスキーマ変更なし。マイグレーション不要です。

「ファーストダウン最速ターゲット指名手配ガチャ」等のターゲット指定型(target_pick)を、管理者が`/plans/[slug]/edit`から編集できるようにしました。中身はトリガー型等と同じ「テキストリストから抽選」の仕組みですが、`source: "custom_text"`という抽選ロジック側の内部フラグを壊さないよう専用の更新処理（`app/plans/[slug]/edit/target-pick-actions.ts`）にしています。

対象は運営キュレーション企画のみ（ユーザーは新規作成不可、既存の編集フォームで足りるため新規作成フォームは追加していません）。

次は抽選型（ランダムセレクト等）に進みます。
